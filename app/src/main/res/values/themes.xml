<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.VgroupStore" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Disable Material3 automatic button coloring -->
        <item name="materialButtonStyle">@style/Widget.AppCompat.Button</item>
        <item name="buttonStyle">@style/Widget.AppCompat.Button</item>

        <!-- Optional: remove tint everywhere -->
        <item name="android:colorButtonNormal">@android:color/transparent</item>

        <!-- Your colors -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/accentColor</item>

    </style>
</resources>